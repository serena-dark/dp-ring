# t1w1s3 · 校验

- `npm ci`（或 `npm install`）
- `npm run build`
- `npm run typecheck`
- `npm run lint`
- 可选：`docker build -t dp-ring .` 冒烟

将结果记入 `logs/YYYY-MM-DD.log`（若需要）。

> 完成勾选请在 [r1m1](../../../requirements/r1-prototype-baseline/milestones/r1m1-repository-and-docs-baseline.md) 中更新。
