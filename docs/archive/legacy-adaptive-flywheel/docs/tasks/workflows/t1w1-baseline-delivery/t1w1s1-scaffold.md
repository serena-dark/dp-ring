# t1w1s1 · 脚手架与根文档

- 添加根 `.gitignore`（`node_modules`、`dist`、`.env*`、编辑器等）。
- 撰写 `INDEX.md`、`VALUE.md`，更新 `README.md`（脚本与 Docker 示例）。
- 将 `package.json` 中 `engines.node` 对齐为 `>=22.19.0`。

> 完成勾选请在 [r1m1](../../../requirements/r1-prototype-baseline/milestones/r1m1-repository-and-docs-baseline.md) 中更新。
