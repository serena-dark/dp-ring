# {{TITLE}}

本文档为需求包 **{{BASE_NAME}}** 的主说明；目录与 prototype 中 `r*-requirement-name` 对齐。

> 创建日期（仅写在正文）：{{DATE_ISO}}

## 参考

见子目录 `references/`。

## 里程碑

见 `milestones/`（含 `prerequisites.md`）。

## Session ID

若本需求包绑定某轮执行，文首补 **`session_id`**（与任务/工作流互文同源）；含义见 `docs/sessions/sessions.md`，何时标注与字面值形态见 `docs/documentation-standards.md`。
