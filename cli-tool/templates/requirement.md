# {{TITLE}}

> Legacy template: 本模板用于旧 Adaptive Flywheel 文档流中的 requirement 文档，不是 `dp-ring v2` 当前主模板。

本文档为需求包 **{{BASE_NAME}}** 的主说明；目录与 prototype 中 `r*-requirement-name` 的旧约定对齐。

> 创建日期（仅写在正文）：{{DATE_ISO}}

## 参考

见子目录 `references/`。

## 里程碑

见 `milestones/`（含 `prerequisites.md`）。

## Session ID

若本需求包绑定某轮旧执行，文首补 **`session_id`**（与任务/工作流互文同源）；见 `docs/archive/legacy-adaptive-flywheel/docs/sessions/sessions.md`；其它 legacy 路径基名见 `docs/documentation-standards.md`。
